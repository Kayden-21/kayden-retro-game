import React from "react";
import {HeaderContainer, MainHeading} from "./Header.styles";

export const Header:React.FC = () => {

    return(
        <HeaderContainer>
            <MainHeading>The Bachelor (Kayden edition)</MainHeading>
        </HeaderContainer>
    );
}
